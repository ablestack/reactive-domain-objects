{
  "private": true,
  "name": "@ablestack/reactive-domain-graphs",
  "version": "0.1.0",
  "description": "A suite of packages to facilitate building and running graphs of Reactive Domain Objects",
  "author": "jamshally",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ablestack/graphql-codegen-hasura"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "tsc --build --verbose",
    "rebuild": "tsc --build --force --verbose",
    "lpub": "lerna publish --forcePublish"
  },
  "devDependencies": {
    "lerna": "^3.20.0",
    "ts-node": "~7.0.0",
    "typescript": "^3.7.2"
  }
}
